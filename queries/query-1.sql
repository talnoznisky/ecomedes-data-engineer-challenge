--
-- Get the total amount spent across all invoices broken out by category.
--
-- Output columns:
-- - category_ident::TEXT
-- - total_dollars::INT
--

-- Write your query here:

SELECT NULL::TEXT AS category_ident
     , NULL::INT AS total_dollars
 WHERE FALSE
